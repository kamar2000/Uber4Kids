import 'package:flutter/cupertino.dart';

class QAMER extends StatelessWidget {
  const QAMER({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
